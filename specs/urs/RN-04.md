# RN-04: No Duplicates
**Category:** Core Execution (TSK-CORE)

**Description:**
A new `/start` sequence cannot be started if a task shift is already active for the same user. The user will be requested to cancel the active shift to prevent overlapping bugs.

**Implementation Status:** Implemented in `specs/changes/TSK-CORE-02`
