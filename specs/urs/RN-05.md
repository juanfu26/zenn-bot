# RN-05: Safe Interruption
**Category:** Core Execution (TSK-CORE)

**Description:**
The `/cancel` command stops the wait timer immediately and gracefully aborts the remaining execution tracks (e.g. Sign-Out) preventing logging a shift end.

**Implementation Status:** Implemented in `specs/changes/260305-safe-interruption`
