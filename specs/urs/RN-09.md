# RN-09: Timezone Awareness
**Category:** Environment Configuration (TSK-CFG)

**Description:**
The system will operate strictly within the `Europe/Madrid` timezone, automatically handling daylight saving time (DST) changes through OS-level `tzdata` configuration injected into the Docker daemon to ensure consistent workhour calculation.

**Implementation Status:** Implemented in `specs/changes/TSK-CFG-01`
